---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: platformagents.kubeagents.x-k8s.io
spec:
  group: kubeagents.x-k8s.io
  names:
    kind: PlatformAgent
    listKind: PlatformAgentList
    plural: platformagents
    singular: platformagent
  scope: Namespaced
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: PlatformAgent is the Schema for the platformagents API
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: spec defines the desired state of PlatformAgent
              properties:
                deployment:
                  description: Deployment abstracts the Kubernetes Pod/Deployment configuration.
                  properties:
                    image:
                      description: Image specifies the container image repository.
                      type: string
                    imagePullPolicy:
                      default: IfNotPresent
                      description:
                        ImagePullPolicy specifies if the image should be
                        pulled.
                      enum:
                        - Always
                        - Never
                        - IfNotPresent
                      type: string
                    tag:
                      default: latest
                      description: Tag specifies the container image tag.
                      type: string
                  required:
                    - image
                  type: object
                harness:
                  description:
                    Harness configures the core execution environment and
                    framework-level settings.
                  properties:
                    clusterName:
                      description:
                        ClusterName is the logical name of the cluster where
                        the agent is running.
                      type: string
                    hermes:
                      description:
                        Hermes configures the internal event-routing or agent
                        framework.
                      properties:
                        apiServerSecretRef:
                          description:
                            ApiServerSecretRef securely references a Secret
                            containing the API_SERVER_KEY.
                          properties:
                            key:
                              description:
                                The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description:
                                Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                            - key
                          type: object
                          x-kubernetes-map-type: atomic
                        dashboardEnabled:
                          default: true
                          description:
                            DashboardEnabled toggles the PLATFORM_AGENT_DASHBOARD
                            environment variable.
                          type: boolean
                        platformAgentHome:
                          default: /opt/data
                          description:
                            PlatformAgentHome is the path to the PLATFORM_AGENT_HOME
                            directory.
                          type: string
                        pluginsDebug:
                          default: false
                          description:
                            PluginsDebug toggles the PLATFORM_AGENT_PLUGINS_DEBUG
                            environment variable.
                          type: boolean
                      type: object
                    location:
                      description: Location is the geographical location or cloud region.
                      type: string
                  type: object
                integration:
                  description: Integration configures platform-specific external connections.
                  properties:
                    googleChat:
                      description: GoogleChat configures the Google Chat integration.
                      properties:
                        allowedUsers:
                          description:
                            AllowedUsers is a list of allowed users. If not
                            present, all users will be allowed.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        enabled:
                          default: false
                          description: Enabled toggles the Google Chat integration.
                          type: boolean
                        homeChannel:
                          description: HomeChannel is the home channel Chat address.
                          type: string
                        projectId:
                          description: ProjectID is the target GCP Project ID for Pub/Sub.
                          type: string
                        subscriptionName:
                          description:
                            SubscriptionName is the GCP Chat Subscription
                            Name.
                          type: string
                        topicName:
                          description: TopicName is the GCP Chat Topic Name.
                          type: string
                      type: object
                      x-kubernetes-validations:
                        - message:
                            projectId, topicName, and subscriptionName are required
                            when Google Chat integration is enabled
                          rule:
                            "!has(self.enabled) || self.enabled == false || (has(self.projectId)
                            && has(self.topicName) && has(self.subscriptionName))"
                  type: object
                model:
                  description: Model configures the LLM reasoning backend.
                  properties:
                    default:
                      description: Default is the primary model to use (e.g., "gemini-3.1-flash-lite").
                      minLength: 1
                      type: string
                    gemini:
                      description: Gemini configures the Gemini provider.
                      properties:
                        apiKeySecretRef:
                          description:
                            ApiKeySecretRef securely references a Secret
                            containing the GEMINI_API_KEY.
                          properties:
                            key:
                              description:
                                The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description:
                                Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                            - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                    provider:
                      description: Provider is the active AI provider (e.g., "gemini").
                      minLength: 1
                      type: string
                  required:
                    - default
                    - provider
                  type: object
                security:
                  description:
                    Security configures RBAC, Pod Security, and Workload
                    Identity.
                  properties:
                    serviceAccountName:
                      description:
                        ServiceAccountName is the Kubernetes Service Account
                        bound to the Deployment.
                      type: string
                    workloadIdentity:
                      description: WorkloadIdentity maps external IAM to the KSA securely.
                      properties:
                        gcp:
                          description: Gcp configures Google Cloud Workload Identity.
                          properties:
                            gsaName:
                              description: GSAName is the Google Service Account Name.
                              type: string
                            projectId:
                              description:
                                ProjectID is the GCP Project ID mapping the
                                GSA to the KSA.
                              type: string
                          type: object
                      type: object
                  type: object
              type: object
            status:
              description: status defines the observed state of PlatformAgent
              properties:
                conditions:
                  description:
                    Conditions represent the latest available observations
                    of the instance's state.
                  items:
                    description:
                      Condition contains details for one aspect of the current
                      state of this API Resource.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is the last time the condition transitioned from one status to another.
                          This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message is a human readable message indicating details about the transition.
                          This may be an empty string.
                        maxLength: 32768
                        type: string
                      observedGeneration:
                        description: |-
                          observedGeneration represents the .metadata.generation that the condition was set based upon.
                          For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                          with respect to the current state of the instance.
                        format: int64
                        minimum: 0
                        type: integer
                      reason:
                        description: |-
                          reason contains a programmatic identifier indicating the reason for the condition's last transition.
                          Producers of specific condition types may define expected values and meanings for this field,
                          and whether the values are considered a guaranteed API.
                          The value should be a CamelCase string.
                          This field may not be empty.
                        maxLength: 1024
                        minLength: 1
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                        type: string
                      status:
                        description: status of the condition, one of True, False, Unknown.
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                        type: string
                      type:
                        description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                        type: string
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    type: object
                  type: array
                deploymentStatus:
                  description: DeploymentStatus tracks the state of the underlying compute.
                  properties:
                    name:
                      description:
                        Name is the exact name of the underlying Kubernetes
                        Deployment.
                      type: string
                    readyReplicas:
                      description:
                        ReadyReplicas indicates how many replicas are fully
                        ready.
                      format: int32
                      type: integer
                  type: object
                lastReconcileTime:
                  description:
                    LastReconcileTime is the timestamp when the operator
                    last updated this status.
                  format: date-time
                  type: string
                phase:
                  description:
                    Phase is the overall state (Pending, Provisioning, Ready,
                    Failed).
                  type: string
                serviceStatus:
                  description: ServiceStatus holds internal/external endpoints.
                  properties:
                    endpoint:
                      description: Endpoint is the primary URL or IP to reach the agent.
                      type: string
                  type: object
                storageStatus:
                  description: StorageStatus tracks PVC binding state.
                  properties:
                    bound:
                      description:
                        Bound indicates if the primary PVC has been successfully
                        provisioned.
                      type: boolean
                  type: object
              type: object
          required:
            - spec
          type: object
      served: true
      storage: true
      subresources:
        status: {}
