# clusters/cluster-a/namespaces/team-x/

Namespace-scoped desired state for the `team-x` tenant: `Namespace`, tenant RBAC, `NetworkPolicy`
(default-deny per Phase 3), `ResourceQuota`, and workloads. Applied by `cluster-a`'s pipeline on
merge. The Developer Team Agent for this namespace proposes changes here via PR (read-only otherwise).
